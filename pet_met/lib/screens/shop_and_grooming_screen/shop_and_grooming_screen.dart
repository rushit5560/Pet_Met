import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ShopAndGroomingScreen extends StatelessWidget {
  const ShopAndGroomingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          // Positioned(
          //   top: Get.height * 0.06,
          //   left: Get.width * 0.12,
          //   child: Image.asset(
          //     "assets/images/petmet_logo.png",
          //     width: Get.width * 0.25,
          //   ),
          // ),
        ],
      ),
    );
  }
}
