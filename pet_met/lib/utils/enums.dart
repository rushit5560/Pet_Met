enum AppBarOption {
  none,
  singleBackButtonOption,
  drawerButtonOption,
}

enum PetOption {
  addOption,
  updateOption,
}

enum ProfileChangeOption {
  stay, back
}

enum StoryOption {
  moveForward, stay
}